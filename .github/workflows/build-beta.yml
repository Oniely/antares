name: Build & release [BETA]

on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+-beta.[0-9]+'

env:
  BRANCH_NAME: ${{ github.head_ref || github.ref_name }}

jobs:
  release:
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        os: [macos-latest, ubuntu-latest, windows-latest]

    steps:
      - name: Check out Git repository
        uses: actions/checkout@v4
        with: 
          ref: beta

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
      
      - name: Install dependencies
        run: | 
          npm i
          npm install "dmg-license" --save-optional

      - name: Fix missing nested deps in ajv-formats (Windows)
        if: matrix.os == 'windows-latest'
        run: |
          $base = "node_modules\ajv-formats\node_modules"
          foreach ($pkg in @("fast-deep-equal", "fast-uri")) {
            $src = "node_modules\$pkg"
            $dest = "$base\$pkg"
            if (!(Test-Path $dest)) { Copy-Item -Recurse $src $dest }
          }
        shell: pwsh
        
      - name: "Build"
        run: npm run build

      - name: Release
        uses: ncipollo/release-action@v1
        with:
          artifacts: "build/*.AppImage,build/*.yml,build/*.deb,build/*.dmg,build/*.blockmap,build/*.zip,build/*.exe"
          allowUpdates: true
          draft: true
          generateReleaseNotes: true
